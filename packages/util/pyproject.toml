[tool.poetry]
name = "util"
version = "0.1.0"
description = "Utility functions for Cypherock SDK"
authors = ["Temiloluwa Yusuf <ytemiloluwa@gmail.com>"]
readme = "README.md"
packages = [{include = "src/util"}]

[tool.poetry.dependencies]
python = ">=3.11"
pyserial = "^3.5"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"


